# dependencies
node_modules/

# build output
dist/
.astro/
*.tsbuildinfo

# Generated from tokens/design-tokens.json by `npm run tokens`, which runs on
# predev and prebuild. Committing them invites edits to the copy instead of
# the source, which is the one thing the token pipeline exists to prevent.
src/styles/tokens.css
src/lib/tokens.generated.ts

# deploy
.vercel

# environment
.env
.env.*
!.env.example

# os
.DS_Store
